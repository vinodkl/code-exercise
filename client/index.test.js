import expect from 'expect';

// Just a checkpoint to make sure the TEST environment is functional
describe('Test environment', () => {
  it('should be working', () => {
    expect(true).toEqual(true);
  });
});
